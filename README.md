백엔드 -> server
프론트엔드 -> client

server/config -> 앱 실행
../model -> DB 관련
../router -> 엔드포인트

pipfile 써서 의존성 버전 맞추고

깃헙 사용하기 힘들다 싶으면 이상한 짓 하지말고 바로 질문방에 질문 ㄱㄱ


# 1. 최신 먼저 받기 (충돌 미리 방지)  ***** 중요
git pull origin main

# 2. 수정 작업
# (코드 작성)

# 3. 변경 내용 확인
git status
git diff

# 4. 커밋 & 푸시
git add .
git commit -m "뭐 했는지 한 줄로"
git push origin main

간단한 깃헙 사용법이니 저 절차 무조건 지켜주세요 제발
